# Secrets — never commit
.env
.env.local

# dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# logs
*.log

# NOTE: pipeline/ IS committed on purpose. The scoping docs, specs, changelogs
# and diffs are the actual work product — the audit trail this agent exists to
# produce. Only real secrets are excluded.
